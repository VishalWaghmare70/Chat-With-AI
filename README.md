Create a ".env" nmaed file and add there your api key of OpenAI as
	OPENAI_API_KEY=sk-prj-YOUR API KEY
What you need

Python 3.8 or newer
A virtual environment (recommended)
pip packages: flask, python-dotenv, openai
(Optional) waitress for running in production on Windows
PowerShell commands
python -m pip install --upgrade pip
pip install flask python-dotenv openai
pip install waitress
pip install flask python-dotenv openai

